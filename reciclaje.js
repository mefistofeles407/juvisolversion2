// Activar la cuenta al hacer clic
const activarCuentaButton = document.getElementById('activarCuenta');

activarCuentaButton.addEventListener('click', () => {
  // Redirige a la página de activación
  window.location.href = 'activacion.html';
});